---
description: 
globs: 
alwaysApply: true
---

# Tailwind & Classnames

- Enforce tailwind styling using classnames wherever possible.
- Avoid using template literal syntax and use cx() from `classnames` instead.
- Import cx using `import { cx } from '@/utils/cx'`.
